---
import slug from 'slug';
import type { PersonneRecord } from '../pocketbase-types'

interface Props {
    personne: PersonneRecord
}
const personne = Astro.props.personne;
---
<a href = {`/personnes/${personne.id}-${slug(personne.nom ?? "Inconnu")}`}>{personne.nom}</a>